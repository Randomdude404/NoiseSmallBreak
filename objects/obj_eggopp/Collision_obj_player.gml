/// @description Insert description here
// You can write your code in this editor
if state == 0 && (other.movespeed >= 12.1 || ( other.state = states.slide || other.state = states.bounce || other.state = states.fly || other.state = states.pound))
    instance_destroy()
if state == 1 && other.state = states.slide || other.sprite_index = other.spr_bounce
    instance_destroy()
if state == 1 && !( other.state = states.slide || other.sprite_index = other.spr_bounce || other.state = states.fly || other.state = states.pound)
   obj_player.scr_hurtplayer() 
if state == 1 && (other.state = states.fly || other.state = states.pound)
    instance_destroy()



